const Manage = (name, manager, openEveryday) => ({
  name, manager, openEveryday
});

const manages = [
  Manage("Beltline Center", "James", false),
  Manage("Georgia TEch", "Mj", true),
  Manage("Piedmont Park", "Adam", true)
];

export default manages;